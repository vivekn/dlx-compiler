## Python prototype for topological sorting using depth first search.

adj_matrix = [[]]


def find_independent_nodes(matrix):
    return

def t_sort(matrix):
    S = find_independent_nodes(matrix)
    
